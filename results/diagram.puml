@startuml
title Create New Employee Profile Workflow

start

:Click on "Create Employee";

repeat
  :Fill in the required details;
repeat while (Fields required) is (yes)

// Acceptance criteria: All required fields must be filled before submission
if (All required fields filled?) then (yes)
  :Submit the form;
  // Acceptance criteria: Success notification after saving
  :Show success notification;
else (no)
  repeat while (All required fields filled?) is (no)
    :Prompt to fill all required fields;
  end repeat
endif

-> No
:Option to cancel and return without saving;
:Return to main menu;

end
@enduml